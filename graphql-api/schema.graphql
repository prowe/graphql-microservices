schema {
    query: Query
    mutation: Mutation
}

type Query {
    """We need something because an empty type isn't valid """
    messageOfTheDay: String!
}

type Mutation {
    """We need something because an empty type isn't valid """
    noOp: String!
}